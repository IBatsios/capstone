import React, { Component } from "react";
import { Header } from "app";

class User extends Component {
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
}

export default User;
